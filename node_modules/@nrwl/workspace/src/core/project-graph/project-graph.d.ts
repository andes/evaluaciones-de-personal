import { ProjectGraph } from './project-graph-models';
import { FileData } from '../file-utils';
import { FileMap } from '../file-graph';
export declare function createProjectGraph(workspaceJson?: any, nxJson?: import("../shared-interfaces").NxJson<string[] | "*">, workspaceFiles?: FileData[], fileRead?: (s: string) => string, cache?: false | {
    data: ProjectGraphCache;
    mtime: number;
}): ProjectGraph;
interface ProjectGraphCache {
    projectGraph: ProjectGraph;
    fileMap: FileMap;
}
export {};
