/**
 * Creates a RegExp that matches the words in the search string.
 *
 * @export
 * @param {string} search_string Search string
 * @param {object} options Opciones de configuracion
 * @param {boolean} options.startWith La palabra debe comenzar con la expression dada
 * @returns {RegExp}
 */
export declare function makePattern(search_string: string, options?: any): RegExp;
