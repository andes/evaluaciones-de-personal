"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("./utils");
function matchDate(date) {
}
exports.matchDate = matchDate;
function partialString(value, compareValue) {
    if (value && value.length > 0 && value.split('^')) {
        const [, expression] = value.split('^');
        let reg = utils_1.makePattern(expression);
        return reg.test(compareValue.toString());
    }
    else {
        return (value === compareValue);
    }
}
exports.partialString = partialString;
function matchString(value, compareValue) {
    return (value === compareValue);
}
exports.matchString = matchString;
function buildQuery(query, searchSpecification, value) {
    return Object.keys(query).reduce((flag, key) => {
        if (query[key] && value[key]) {
            return flag && (searchSpecification[key](query[key], value[key]));
        }
        return false;
    }, true);
}
exports.buildQuery = buildQuery;
exports.MemoryQuery = {
    matchDate,
    partialString,
    matchString,
    buildQuery
};
//# sourceMappingURL=in-memory.js.map