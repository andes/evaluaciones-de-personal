"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
var core_1 = require("@angular/core");
var SexoPipe = /** @class */ (function () {
    function SexoPipe() {
    }
    SexoPipe.prototype.transform = function (value) {
        var result;
        var sexo = value && value.sexo && (value.sexo.nombre || value.sexo);
        var genero = value && value.genero && (value.genero.nombre || value.genero);
        if (!genero) {
            result = sexo;
        }
        else {
            if (genero.toUpperCase() === sexo.toUpperCase()) {
                result = sexo;
            }
            else {
                result = genero + ' (autopercibido)';
            }
        }
        if (result) {
            return result.charAt(0).toUpperCase() + result.substr(1).toLowerCase();
        }
        else {
            return null;
        }
    };
    SexoPipe = __decorate([
        core_1.Pipe({ name: 'sexo', pure: false })
    ], SexoPipe);
    return SexoPipe;
}());
exports.SexoPipe = SexoPipe;
//# sourceMappingURL=sexo.pipe.js.map